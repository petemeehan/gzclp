import React from 'react';

import { colours } from 'gzclp/js/styles';

import Ionicons from 'react-native-vector-icons/Ionicons';
import FontAwesome from 'react-native-vector-icons/FontAwesome';

export const navArrow = <Ionicons name="ios-arrow-forward" size={22} color={colours.primaryColour} />;
export const menuTick = <Ionicons name="md-checkmark-circle" size={26} color={colours.primaryColour} />;
export const cogIcon = <FontAwesome name="cog" size={28} color='white' />;
